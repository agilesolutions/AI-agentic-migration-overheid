apiVersion: v1
kind: Service
metadata:
  name: {{ include "notebook.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "notebook.fullname" . }}
    app.kubernetes.io/component: "frontend"
  ports:
    - port: 8080
      targetPort: 8080
  type: {{ .Values.service.type }}